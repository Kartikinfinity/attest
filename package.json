{
  "name": "attest",
  "version": "0.1.0",
  "private": true,
  "description": "Execution-based MCP server behavior verification system",
  "workspaces": [
    "apps/web",
    "packages/verdict-engine"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=packages/verdict-engine && npm run build --workspace=apps/web",
    "test": "npx vitest run",
    "test:watch": "npx vitest",
    "lint": "npm run lint --workspace=apps/web"
  },
  "license": "MIT",
  "engines": {
    "node": ">=22.14.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "better-sqlite3": "^13.0.3",
    "tsx": "^4.23.12",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@truefoundry/trueforge-sdk": "^0.1.3"
  }
}
